projection implementation in class ZBP_C_DT_HEADER_TOKEN unique;
strict ( 2 );
use draft;
define behavior for ZC_DT_HEADER_TOKEN alias ZcDtHeaderToken
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}